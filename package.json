{
  "name": "ai-assessment-assistant-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron/main.js",
  "description": "Teacher desktop shell for assessment and assignment workflows.",
  "scripts": {
    "assessment:dev": "npm run dev --prefix assessment",
    "assessment:build": "npm run build --prefix assessment",
    "assignment:dev": "npm run dev --prefix assignment",
    "assignment:build": "npm run build --prefix assignment",
    "python:setup": "bash scripts/setup-python-env.sh",
    "dmg:background": "node scripts/create-dmg-background.js",
    "electron:dev": "npm run assessment:build && npm run assignment:build && electron .",
    "electron:assignment": "npm run assessment:build && npm run assignment:build && APP_MODE=assignment electron .",
    "electron:pack": "npm run assessment:build && npm run assignment:build && electron-builder --dir",
    "electron:dist": "npm run dmg:background && npm run assessment:build && npm run assignment:build && electron-builder",
    "build:mac": "npm run dmg:background && npm run assessment:build && npm run assignment:build && electron-builder --mac --dir && bash scripts/create-dmg-mac.sh",
    "build:mac:app": "npm run assessment:build && npm run assignment:build && electron-builder --mac --dir",
    "build:mac:dmg": "npm run dmg:background && bash scripts/create-dmg-mac.sh",
    "build:windows:full": "powershell -ExecutionPolicy Bypass -File scripts/build-windows.ps1",
    "build:windows": "npm run assessment:build && npm run assignment:build && electron-builder --win --x64"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "adm-zip": "^0.5.17",
    "cheerio": "^1.2.0",
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.18.3",
    "markdown-it": "^14.2.0",
    "multer": "^2.1.1",
    "pdf-lib": "^1.17.1",
    "pngjs": "^7.0.0",
    "unzipper": "^0.12.3"
  },
  "devDependencies": {
    "@types/pngjs": "^6.0.5",
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3",
    "sharp": "^0.34.5"
  },
  "build": {
    "appId": "io.github.bloomgloom.ai-assessment-assistant",
    "productName": "AI Assessment Assistant",
    "afterPack": "scripts/after-pack-sign-mac.cjs",
    "asar": true,
    "asarUnpack": [
      "node_modules/**/*.node",
      "assessment/python/.venv/**/*"
    ],
    "files": [
      "electron/**/*",
      "assessment/server/dist/**/*",
      "assessment/client/dist/**/*",
      "assignment/server/dist/**/*",
      "assessment/python/**/*",
      "package.json"
    ],
    "directories": {
      "output": "release"
    },
    "mac": {
      "target": "dir"
    },
    "win": {
      "target": "nsis"
    }
  },
  "allowScripts": {
    "sharp@0.34.5": true
  }
}
