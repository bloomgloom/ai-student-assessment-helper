services:
  assessment:
    build: .
    ports:
      - "127.0.0.1:3001:3001"
    environment:
      APP_STORAGE_DIR: /app/storage
      PORT: 3001
    volumes:
      - ./storage:/app/storage
    restart: unless-stopped
